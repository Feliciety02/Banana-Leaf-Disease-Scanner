# Acquisition workflows

- `davao-field/` holds registry, review, and generated manifest state for the
  locked Davao field evaluation set.
- `ssl/` holds source registry, review, and generated manifest state for
  external self-supervised-learning images.

Original images remain in the clearly named input roots at the dataset top
level: `../davao-field/` and `../ssl-unlabeled/`.
